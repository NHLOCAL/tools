# הוספה והסרת המספר המחייג מרשימת התפוצה - template_add_number/template_remove_number

Source kind: `f2`
Topic ID: `58`
Topic source: https://f2.freeivr.co.il/topic/58

## Post 0 — ימות המשיח — topic author

Post ID: `78`
Published: `2020-05-18T18:31:07.176Z`
Updated: `2024-01-23T15:29:58.119Z`
Source: https://f2.freeivr.co.il/post/78

## [](https://f2.freeivr.co.il)הוספת המספר המחייג לְרשימת תפוצה

מודול זה מוסיף או מסיר את המספר המחייג דהיינו את מספר הטלפון שחייג ונכנס לשלוחה, ניתן להשתמש במודול [הזה](https://f2.freeivr.co.il/post/79) כדי להסיר או להוסיף מספרי טלפון אחרים

סוג השלוחה

```
type=template_add_number

```

#### [](https://f2.freeivr.co.il)קישורים לתוספים שניתנים להגדרה בכל מודול

>

##### [](https://f2.freeivr.co.il)[רשימת כל ההגדרות שניתנות להטמעה בכל מודול](https://f2.freeivr.co.il/post/78269)

##### [](https://f2.freeivr.co.il)

##### [](https://f2.freeivr.co.il)[הגדרות זיהוי בכניסה לשלוחה](https://f2.freeivr.co.il/post/1250)

##### [](https://f2.freeivr.co.il)

##### [](https://f2.freeivr.co.il)[הסבר על הודעת ברוכים הבאים](https://f2.freeivr.co.il/post/77546) (הודעה ראשונה בשלוחה)

##### [](https://f2.freeivr.co.il)[הרשאות כניסה לשלוחה](https://f2.freeivr.co.il/post/77543)

ההוספה תתבצע בקמפיין "ברירת מחדל" להגדרת קמפין מסויים יש להגדיר:

```
template_to_add=2

```

קמפיין ראשון מספר אחד, קמפיין שני מספר 2 וכו' בדוגמה מוסיף לקמפיין 2
במידה ורוצים להוסיף לכמה רשימות תפוצה בבת אחת, יש לרשום:

```
template_to_add=5,6,10,2

```

בדוגמה מוסיף לקמפיינים 5,6,10,2

ניתן גם לרשום :

```
template_to_add=all

```

ואז ההוספה תתבצע בכל הרשימות.

שימו לב:
לאחר ההוספה, המערכת תשמיע סיכום של הרשימות שאליהם נוסף המספר, באופן הבא:
המערכת תשמיע הודעה:
`M1984` הוספתם את הטלפון שלכם לרשימות התפוצה שמספרם, 5,6,10,2
ניתן להשמיע שמות לרשימות התפוצה, במקום המספרים;
כדי לבצע את זה, יש להעלות קובץ לשלוחה, ולשנות את שמו, לשם מספר רשימת התפוצה.
לדוגמא:
רשימת תפוצה 1 היא: עדכונים כלליים.
רשימת תפוצה 2 היא: עדכונים דחופים.
רשימת תפוצה 3 היא: הודעות חדשות.

כדי להשמיע "הוספתם את הטלפון שלכם לרשימות התפוצה, עדכונים כלליים, עדכונים דחופים, הודעות חדשות"

יש להעלות לשלוחה 4 קבצי שמע בשמות הבאים:
`M1984` הוספתם בהצלחה את הטלפון שלכם לרשימות התפוצה:
שם קובץ עדכונים כלליים = 1
שם קובץ עדכונים דחופים = 2
שם קובץ הודעות חדשות = 3

בכל מקרה, אם משתמשים בהוספה לכמה רשימות בבת אחת, המערכת לא משמיעה את מספר הטלפון של המצטרף.

ברירת מחדל מוסיף את המספר ללא בקשת אישור, אם רוצים שהמערכת תבקש אישור לפני הוספת המספר, יש להגדיר

```
template_add_ask=yes

```

ואז המערכת תשמיע, `M0026` "הינכם מבקשים להוסיף לרשימת התפוצה את טלפון מספר", `M0028` "לאישור ההרשמה הקישו 1, לביטול הקישו 2", ואם מקישים 2 המערכת תשמיע `M0027` "הרשמתכם בוטלה".

אם רוצים שלאחר ההוספה לא יודיע "הוספתם בהצלחה את טלפון מספר .. והשמעת המספר " אלא רק הודעה פשוטה של "הוספתם את הטלפון שלכם לרשימת התפוצה"
(במידת הצורך בשביל ליצור שקט לגמרי ניתן להחליף את הקובץ `M1418` להודעה שקטה)
יש לעדכן:

```
template_add_ok_no_say_number=yes

```

במידה והגדרתם הוספה לכמה רשימות וברצונכם שישמיע `M1418` יש להגדיר

```
template_add_ok_no_say_list_number=yes

```

לאחר סיום ההוספה ברירת מחדל חוזר שלב אחד אחורה ניתן להגדיר מעבר לשלוחה אחרת או ניתוק | ראה [כאן](https://f2.freeivr.co.il/post/77476) את האפשרויות להגדרה.

```
end_goto=/8

```

בדוגמא המאזין יעבור לשלוחה 8 שתחת התפריט הראשי.

הודעות קשורות:
`M1015`מספר הטלפון שלכם אינו מזוהה
`M1023` לא ניתן לצרף את טלפון
`M1024` הוספתם בהצלחה לרשימת התפוצה את טלפון מספר
`M1026` שלום ותודה
`M1418` הוספתם בהצלחה את מספר הטלפון שלכם לרשימת התפוצה
`M1984` הוספתם את הטלפון שלכם לרשימות התפוצה שמספרם, 1,2,3
`M1985` לא ניתן לצרף את הטלפון שלכם לרשימת תפוצה, X
`M0026` הינכם מבקשים להוסיף לרשימת התפוצה את טלפון מספר
`M0028` לאישור ההרשמה הקישו 1, לביטול הקישו 2
`M0027` הרשמתכם בוטלה

## Post 1 — שמחה - זו הסיסמא — community reply

Post ID: `68258`
Published: `2021-09-20T12:11:31.528Z`
Updated: `2024-05-09T14:00:48.875Z`
Source: https://f2.freeivr.co.il/post/68258

## [](https://f2.freeivr.co.il)הסרת המספר המחייג מרשימת התפוצה

סוג השלוחה

```
type=template_remove_number

```

שימו לב - כברירת מחדל המערכת משאירה את המספר כ'חסום' ברשימה, אבל לא תמחק אותו לגמרי מהרשימה.
[בנוסף - המספר ייחסם ברמת המערכת, ולא יקבל הודעות וצינתוקים מהמערכת בכלל].

על מנת שהמספר יימחק לגמרי מהרשימה, יש להוסיף:

```
remove_and_delete=yes

```

ואז המערכת תסיר לגמרי את המספר מהרשימה, וזה לא ישפיע על רשימות אחרות.

#### [](https://f2.freeivr.co.il)קישורים לתוספים שניתנים להגדרה בכל מודול

>

##### [](https://f2.freeivr.co.il)[רשימת כל ההגדרות שניתנות להטמעה בכל מודול](https://f2.freeivr.co.il/post/78269)

##### [](https://f2.freeivr.co.il)

##### [](https://f2.freeivr.co.il)[הגדרות זיהוי בכניסה לשלוחה](https://f2.freeivr.co.il/post/1250)

##### [](https://f2.freeivr.co.il)

##### [](https://f2.freeivr.co.il)[הסבר על הודעת ברוכים הבאים](https://f2.freeivr.co.il/post/77546) (הודעה ראשונה בשלוחה)

##### [](https://f2.freeivr.co.il)[הרשאות כניסה לשלוחה](https://f2.freeivr.co.il/post/77543)

כברירת מחדל - חסימת/מחיקת המספר תתבצע מרשימת התפוצה המוגדרת כ'ברירת מחדל'.

במידה ורוצים לשנות את רשימת התפוצה שממנה יוסר/ייחסם המספר, יש להגדיר את מספר הרשימה:
[מספר הרשימה מופיע בלשונית 'שיגור הודעות' > 'בחירת קמפיין' בעמודה הימנית]

```
template_to_remove=2

```

קמפיין ראשון מספר אחד, קמפיין שני מספר 2 וכו'
בדוגמה הנ"ל - המספר ייחסם/יוסר מרשימת תפוצה 2 בכל מצב, גם אם הרשימה אינה מוגדרת 'ברירת מחדל'.

במידה ורוצים לחסום/להסיר מכמה רשימות תפוצה בבת אחת, יש להגדיר:

```
template_to_remove=5,6,10,2

```

בדוגמה מסיר מרשימות תפוצה 5,6,10,2

ניתן גם להגדיר:

```
template_to_remove=all

```

ואז החסימה/הסרה תתבצע מכל רשימות התפוצה הקיימות במערכת.

שימו לב:
לאחר החסימה/הסרה, המערכת תשמיע סיכום של הרשימות שמהם נחסם/הוסר המספר, באופן הבא:
המערכת תשמיע הודעה:
`M1986` הסרתם את הטלפון שלכם מרשימות התפוצה שמספרם, 5,6,10,2
ניתן להשמיע שמות לרשימות התפוצה, במקום המספרים;
כדי לבצע את זה, יש להעלות קובץ לשלוחה, ולשנות את שמו, לשם מספר רשימת התפוצה.
לדוגמא:
רשימת תפוצה 1 היא: עדכונים כלליים.
רשימת תפוצה 2 היא: עדכונים דחופים.
רשימת תפוצה 3 היא: הודעות חדשות.

כדי להשמיע "הסרתם את הטלפון שלכם מרשימות התפוצה, עדכונים כלליים, עדכונים דחופים, הודעות חדשות"

יש להעלות לשלוחה 3 קבצי שמע בשמות הבאים:

שם קובץ עדכונים כלליים = 1
שם קובץ עדכונים דחופים = 2
שם קובץ הודעות חדשות = 3

בכל מקרה, אם משתמשים בהסרה מכמה רשימות בבת אחת, המערכת לא משמיעה את מספר הטלפון של המתקשר שהוסר.

לאחר ההסרה ברירת מחדל חוזר שלב אחד אחורה ניתן להגדיר מעבר לשלוחה אחרת או ניתוק | ראה [כאן](https://f2.freeivr.co.il/post/77476) את האפשרויות להגדרה.

```
end_goto=/8

```

בדוגמא המאזין יעבור לשלוחה 8 שתחת התפריט הראשי

אם רוצים לבטל את השאלה בהתחלה "הנכם מבקשים להסיר.. האם הנכם בטוחים" - אלא שיעביר מיידית להסרה ללא שאלות יש לעדכן

```
template_remove_no_ask=yes

```

אם רוצים שלאחר ההסרה לא יודיע "הסרתם בהצלחה את טלפון מספר .. והשמעת המספר " אלא רק הודעה פשוטה של "הסרתם את הטלפון שלכם מרשימת התפוצה" (במידת הצורך בשביל ליצור שקט לגמרי ניתן להחליף את הקובץ `M1417` להודעה שקטה)
יש להגדיר

```
template_remove_ok_no_say_number=yes

```

הודעות קשורות:
`M1015` מספר הטלפון שלכם אינו מזוהה
`M1025` לא ניתן להסיר את טלפון
`M1026` שלום ותודה
`M1027` הסרת מספר. הנכם מבקשים להסיר את טלפון מספר
`M1028` האם הנכם בטוחים? לאישור ההסרה הַקישו1 , לֶביטול הקישו 2, או נתקו את השיחה.
`M1029` הסרת מספר הטלפון בוטלה
`M1030` הסרתם בהצלחה מרשימת התפוצה את טלפון מספר
`M1417` הסרתם את הטלפון שלכם מרשימת התפוצה
`M1986` הסרתם את הטלפון שלכם מרשימות התפוצה שמספרם, 1,2,3
`M1987` לא ניתן להסיר את הטלפון שלכם מרשימת תפוצה, X
